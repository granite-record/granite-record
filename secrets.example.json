{
  "_comment": "Copy this to secrets.json and fill it in. secrets.json is in .gitignore and must never be committed. Nothing in this repository needs a credential except the YouTube Data API, and the General Court's own database credentials are published by them for public use, so they live in probe_db.py in the open.",
  "youtube_api_key": "your YouTube Data API v3 key, from console.cloud.google.com"
}
